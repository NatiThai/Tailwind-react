import React from "react";
import Header from "../../components/Header";

const LandingPage = ({
    ...otherProps
}) => {
    return(
        <div>
            <Header />
        </div>
    );
};

export default LandingPage;